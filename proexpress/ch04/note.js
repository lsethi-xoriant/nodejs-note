/**
 * |-- compression
 *      The compression middleware gzips transferred data.
 *      |-- threshold
 *          the size in kilobits at which to start compression(ie, the minimum size in kilobits  that can go uncompressed)
 *          每次以多少单位（k）去压缩
 *
 */